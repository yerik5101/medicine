<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    <style name="MySherlockTheme" parent="Theme.Sherlock.Light">
        <item name="actionBarStyle">@style/Widget.DEMO.ActionBar</item>  
        <item name="android:actionBarStyle">@style/Widget.DEMO.ActionBar</item>  
    </style>
    
    <!-- 标题栏的样式 -->  
    <style name="Widget.DEMO.ActionBar" parent="Widget.Sherlock.ActionBar.Solid">     
        <item name="background">@color/theme</item>  
        <item name="backgroundStacked">@color/theme</item>  
        <item name="backgroundSplit">@color/theme</item>  
        <item name="titleTextStyle">@style/Widget.DEMO.ActionBar.Title</item>  
          
        <item name="android:background">@color/theme</item>  
        <item name="android:backgroundStacked">@color/theme</item>  
        <item name="android:backgroundSplit">@color/theme</item>  
        <item name="android:titleTextStyle">@style/Widget.DEMO.ActionBar.Title</item>  
    </style>  
      
    <!-- 标题栏的标题样式 -->  
    <style name="Widget.DEMO.ActionBar.Title" parent="@android:TextAppearance.Holo.Widget.ActionBar.Title">  
        <item name="android:textColor">@color/white</item>  
    </style>
    
    
    
    <!--  font -->
    <style name="style_22_ffffff">
        <item name="android:textSize">22sp</item>
        <item name="android:textColor">@color/white</item>
    </style>
    
    <!-- 自定义loading dialog -->  
    <style name="loading_dialog" parent="android:style/Theme.Dialog">  
        <item name="android:windowFrame">@null</item>  
        <item name="android:windowNoTitle">true</item>     
        <item name="android:windowIsFloating">true</item>  
        <item name="android:windowContentOverlay">@null</item>  
        <item name="android:background">#00000000</item>
		<item name="android:windowBackground">@android:color/transparent</item>
		<item name="android:colorBackgroundCacheHint">@null</item>
		<item name="android:windowIsTranslucent">true</item>
    </style> 
</resources>